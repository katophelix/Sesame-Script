import React from 'react';
import { Card, Button, CardTitle, CardText, Row, Col } from 'reactstrap';

const Card = (props) => {
  return (
    
        <Card body>
          <CardTitle></CardTitle>
          <CardText></CardText>
          <Button></Button>
        </Card>
      );
};

export default Card;

